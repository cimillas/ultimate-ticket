# Documentation index

## Start here
- `../README.md` for project overview and quickstart.
- `concepts.md` for domain terms (event, zone, hold, order).
- `api/error-codes.md` for API error codes and meanings.

## Architecture and decisions
- `adr/` for architecture decision records (ADRs).
- `adr/0001-initial-architecture.md` for the initial system layout.
- `adr/0007-auth-sessions.md` for auth/session design.
- `adr/0009-api-versioning.md` for versioning strategy.

## API and contracts
- `contracts/api-inventory.md` for endpoint inventory.
- `contracts/compatibility-checklist.md` for compatibility rules.
- `contracts/breaking-changes.md` for changes log.

## Testing
- `testing/e2e-plan.md` for E2E test plan and usage.

## Observability
- `observability/plan.md` for logging and future metrics.
- `observability/metrics.md` for current Prometheus metrics.

## Roadmaps
- `roadmap.md` for product milestones.
- `microservices/extraction-roadmap.md` for service extraction steps.
